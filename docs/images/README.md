# Screenshots

Drop PNG/WebP files here and update the image paths in the root [`README.md`](../../README.md).

Suggested filenames (match the placeholders in the README):

| File | What to capture |
|---|---|
| `api-token.png` | Metricool **Account Settings → API** tab, User Token visible (blur the secret) |
| `userid-url.png` | Browser address bar with `userId=` and `blogId=` highlighted |
| `n8n-credentials.png` | n8n **Metricool API** credential modal filled in |
| `brand-locator.png` | Metricool node with Brand resource locator open (From List) |
| `workflow-schedule-post.png` | Example workflow: Best Time → Scheduled Post Create |
| `workflow-media-upload.png` | Example workflow: Media Upload → Scheduled Post Create |
| `workflow-analytics.png` | Example workflow: Analytics Get Aggregation |

Keep secrets redacted. Prefer ~1200–1600px wide captures so they stay readable on GitHub.
